import React from 'react';

import classes from './ScheduleContent.css';

const scheduleContent = () => (
  <div className={classes.ScheduleContent}>
    <span>В скором времени программа будет сформирована и опубликована здесь.</span>
  </div>
);

export default scheduleContent;
